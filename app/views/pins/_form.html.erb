<%= form_with(model: pin, local: true) do |form| %>
  <% if pin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pin.errors.count, "error") %> prohibited this pin from being saved:</h2>

      <ul>
      <% pin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, "Foto" %>
    <%= form.text_field :photo, id: :pin_photo, :class=>'form-control', :placeholder=> "URL de la foto" %>
  </div>

    <div class="form-group">
  <div class="field">
    <%= form.label :name, "Descripcion" %>
    <%= form.text_area :description, id: :pin_description,:class=>'form-control',  :placeholder=> "Descripción", :rows=>"3", :cols=> "10" %>
      </div>
    </div>

    <div class="form-group">
  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active, id: :pin_active %>
  </div>
    </div>

    <div class="form-group" >
      <div class="field">
        <%= form.label 'create', hidden:true  %>
        <%= form.text_field :user_id,:value => current_user.id, hidden:true%>
      </div>
    </div>

    <div class="actions">
    <%= form.submit "Crear", :class =>"btn btn-primary"%>
  </div>
<% end %>
